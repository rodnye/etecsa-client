{
  "name": "etecsa-client",
  "private": true,
  "description": "Monorepo no oficial para la api de la Tienda ETECSA",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "format": "prettier --write .",
    "dev:cli": "pnpm --filter @rodny/etecsa-cli dev"
  },
  "packageManager": "pnpm@10.33.2",
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.9.3"
  }
}
